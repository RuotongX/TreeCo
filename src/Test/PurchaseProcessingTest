import React from 'react'
import {shallow} from 'enzyme'
import PurchaseProcessing from '../Pages/PursacheProcessing/index.js'/*Direction of your component*/

test('Test form item NAME exist', () =>{
    it('NAME should contain nothing',()=>{

        /*Set the data that you will input to one component*/
        const item = shallow(<PurchaseProcessing /*the constructor of this component, with the data above*//>)
        expect(item.getFormItem()).toBe(5);
    })
})